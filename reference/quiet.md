# Silence Print Messages from Code Execution

This utility function executes the provided code while suppressing any
print messages. It is useful for running code quietly, especially when
print statements are not needed.

## Usage

``` r
quiet(x)
```

## Arguments

- x:

  Expression or code block to execute silently.

## Value

The result of the executed code, with all print messages suppressed.
