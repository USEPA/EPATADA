# Get TADA Template

This function returns a blank TADA template that can be used as a
starting point to reformat your own custom data set into the TADA
format.

## Usage

``` r
TADA_GetTemplate()
```

## Value

A TADA template with all required columns for the TADA workflow.

## Examples

``` r
TADA_Template <- TADA_GetTemplate()
```
