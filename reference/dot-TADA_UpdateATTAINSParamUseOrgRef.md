# Update EPATADA internal ATTAINS Param/Use-by-Org table (DEV-TIME ONLY) Saves the latest-cycle view by default.

Update EPATADA internal ATTAINS Param/Use-by-Org table (DEV-TIME ONLY)
Saves the latest-cycle view by default.

## Usage

``` r
.TADA_UpdateATTAINSParamUseOrgRef(latest_cycle_only = TRUE)
```

## Arguments

- latest_cycle_only:

  logical; default TRUE
