% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/CriteriaDuration.R
\name{TADA_Analysis_Join_Windows}
\alias{TADA_Analysis_Join_Windows}
\title{Join a WQP + Criteria Table with Start and End Date Windows}
\usage{
TADA_Analysis_Join_Windows(data_w_criteria, join_back = TRUE)
}
\arguments{
\item{.data}{A TADA data frame. The user should run all desired data cleaning,
processing, harmonization, filtering, and handling of censored data functions
prior to running this function.}

\item{windows}{A data.frame of analysis windows with columns
\code{window_start} and \code{window_end} produced by
\code{\link[=TADA_Analysis_DurationAgg]{TADA_Analysis_DurationAgg()}}.}
}
\value{
A data.frame containing the TADA data frame with the aggregated duration
columns: \code{window_start_date} and \code{window_end_date}. Rows are repeated when an
observation date falls into multiple windows. If no dates fall within any
window ranges, the result is empty.
}
\description{
This data frame requires users to have already joined the WQP data frame
with the TADA criteria table which can be generated from \link{TADA_}For each unique DurationUnit and DurationValue found in the
}
\details{
The function converts \code{ActivityStartDate} and \code{ActivityStartDateTime},
to a \code{Date} column type. It performs an inclusive range join where
\code{ActivityStartDate} falls within \verb{[window_start_date, window_end_date]}
which is generated from \link{TADA_Analysis_DurationAgg}
\itemize{
\item The join uses \code{dplyr::join_by(dplyr::between(ActivityStartDate, window_start_date, window_end_date))} and is inclusive of both endpoints.
\item Because this is a many-to-many join, a single observation date can match
multiple windows if those windows overlap; the output will contain one row
per match.
\item Window start/end are reduced to dates before joining; sub-day time
information is ignored for the match criterion.
}
}
\examples{
cal_4d <- TADA_Analysis_DurationAgg(Data_MT_MissoulaCounty, 4, durationUnit = "n-day")

cal_m <- TADA_Analysis_DurationAgg(Data_MT_MissoulaCounty, 1, durationUnit = "n-month")

cal_3m <- TADA_Analysis_DurationAgg(Data_MT_MissoulaCounty, 3, durationUnit = "n-month")

roll_3m <- TADA_Analysis_DurationAgg(Data_MT_MissoulaCounty, 3, durationUnit = "n-month", rolling = TRUE)

Data_MT_MissoulaCounty_Durations <- join_by_date_range(Data_MT_MissoulaCounty, cal_3m)

Data_MT_MissoulaCounty_Durations_roll <- join_by_date_range(Data_MT_MissoulaCounty, roll_3m)

}
\seealso{
\link{TADA_Analysis_DurationAgg} for constructing \code{windows}.
}
